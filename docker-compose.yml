services:
  web:
    build: .
    container_name: rotaverde_frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # Necessário para o Hot Reload funcionar no Windows/WSL
